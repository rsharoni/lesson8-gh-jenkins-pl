pipeline {
    agent any

    stages {
        stage('Hello') {
            steps {
                sh 'touch empty-file.txt'
                echo 'Hello from Pipeline!'
            }
        }
    }
}
