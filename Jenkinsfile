pipeline {
    agent any

    stages {
        stage('Hello') {
            steps {
                touch "empty-file.txt"
                echo '"Hello from Pipeline!'
            }
        }
    }
}
